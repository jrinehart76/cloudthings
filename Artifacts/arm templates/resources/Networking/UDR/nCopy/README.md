#UDR Creation Templates

Templates to create UDR's across multiple subscriptions

Use template.json if each route has independent next hops. Example parameter file is template.parameters.json

Use template.SameNextHop.json if each route will share the same next hop. Example parameter file is template.SameNextHop.parameters.json
